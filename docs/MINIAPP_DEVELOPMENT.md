# Roamly 消费者小程序契约

```yaml
version: 6
updatedAt: 2026-09-04
scope: 消费者页面、交互、接口消费与客户端状态
reviewStatus: accepted
designStatus: 已冻结
demoImplementationStatus: 已实现
transactionExpansionStatus: 未实现
deviceAcceptanceStatus: 未确认
```

## 契约职责

本文只管理 `Roamly-miniapp` 消费者体验。HTTP 线协议由后端运行时 OpenAPI 管理，阶段顺序参考 [四端交付路线图](../../Roamly/docs/roadmap/FOUR_END_DELIVERY_ROADMAP.md)。本文不定义数据库、服务端事务、管理员权限、商户审核或商户经营页面。

## 技术与视觉

- 原生微信小程序、TypeScript、TDesign MiniProgram；API、Service、Store、页面与类型分层。
- 视觉真源是当前 `styles/tokens.wxss`、`styles/global.wxss` 和已实现页面。主色 `#ff5f57`，辅助色 `#8275ff`，主文字 `#242331`，辅助文字 `#8f8d99`，页面背景 `#f4f5fb`，内容面 `#ffffff`，边框 `#ececf3`。
- 保持轻盈社区与本地生活风格、大圆角内容面、柔和紫灰阴影和克制动效。竞品截图只参考下单与券信息结构，不复制其颜色、字体、素材或文案。
- 图标优先使用 TDesign 图标；业务 ID 始终为字符串，金额由分格式化，时间以服务端为准。

## 已实现页面

| 页面 | 消费接口 | 状态 |
|---|---|---|
| 首页推荐/关注 | `/v1/feeds/**` | 已实现 |
| 分区列表/详情 | `/v1/sections/**` | 已实现 |
| 发布与动态详情 | `/v1/media/images`、`/v1/posts/**`、评论与点赞 | 已实现 |
| 附近与商户详情 | `/v1/cities`、`/v1/shop-types`、`/v1/shops/**` | 已实现 |
| 商户点评 | `/v1/shops/{shopId}/reviews/**` | 已实现 |
| 团购商品 | `/v1/shops/{shopId}/voucher-products`、`/v1/voucher-products/{productId}` | 已实现 |
| 订单与券包 Demo | `/v1/users/me/orders/**`、`/v1/users/me/vouchers/**` | 已实现 |
| 用户中心 | `/v1/users/**`、登录、签到和资料 | 已实现 |

## 客户端协议规则

- 成功响应消费 `Result`、`PageResult`、`CursorPageResult`，204 转换为 `data: null`；错误按真实 HTTP 状态和字符串业务码展示。
- 商品详情消费 `{product,shop}`，订单详情消费 `{order,product,shop}`；不兼容旧字段、旧数字状态或裸对象包装。
- `PENDING_PAYMENT`（待支付）、`PAID`（已支付）、`CANCELED`（已取消）、`REFUNDING`（退款中）、`REFUNDED`（已退款）必须使用中文展示文案。
- 401 清理消费者会话并保留草稿和回跳意图；403、404、409、429、503 保留各自语义，不统一转换成网络错误。
- 空数组展示真实空态；只有响应结构不符合契约时报告“响应格式异常”，不得生成假数据。

## 交易扩展

- 阶段 21：四类券列表和详情按 `PACKAGE`（套餐券）、`CASH`（代金券）、`DISCOUNT`（折扣券）、`MULTI_USE`（次卡）展示真实权益、价格、有效期和退款规则。
- 阶段 22：商品页进入确认订单页，提供数量步进器、门市总价、优惠和实付金额；数量上限使用服务端返回事实。
- 阶段 23：提交订单后进入详情，按 `serverTime` 与 `paymentExpireTime` 展示 15 分钟倒计时；dev/test 显示“模拟支付”，归零后回查服务端状态。
- 阶段 24：券详情展示退款资格和单券退款入口；次卡首次使用后不展示自助退款。
- 阶段 27：券详情请求 60 秒动态二维码并按剩余时间刷新；二维码不展示裸券 ID，手输编号只作为只读备用信息。
- 所有高风险命令由 API 层生成并发送 `Idempotency-Key`，页面重复点击期间禁用操作。

## 页面状态

- 每个列表覆盖首次加载、分页加载、真实空态、筛选无结果、失败和重试。
- 每个详情覆盖加载、404、权限失效、状态冲突和刷新后状态变化。
- 倒计时、本地缓存和推送只改善体验，不能自行修改订单、支付、券或退款状态。
- 定位拒绝保留无距离结果；扫码、相机与商户经营异常不属于消费者端页面职责。

## 本端阶段

| 阶段 | 消费者交付 | 状态 |
|---:|---|---|
| 1-14 | 社区、本地生活、订单与券包 Demo | 已实现 |
| 15 | 契约拆分和视觉基线复核 | 未实现 |
| 21 | 四类券展示 | 未实现 |
| 22 | 确认订单与数量计价 | 未实现 |
| 23 | 倒计时、Mock 支付和多份券 | 未实现 |
| 24 | 单券退款 | 未实现 |
| 27 | 动态二维码与前台刷新 | 未实现 |
| 30 | 自动化与真机验收 | 未实现 |

## 验收

- 执行 `npm run build:npm` 和 `npm run verify`，覆盖 API 路径、Bearer 会话、页面状态机、字符串大 ID、金额、倒计时、四类券、退款和二维码刷新。
- 微信开发者工具验证加载、空态、弱网、定位拒绝和登录恢复；Android/iOS 真机结果必须如实保持“未确认”或记录实际结论。
- 截图检查常见手机宽度下无文字溢出、按钮遮挡、底部栏冲突和布局跳动。

## 非目标

本端不包含管理员、商户经营、扫码核销、员工、佣金配置、结算重试、真实微信支付回调、生产短信、搜索、通知、举报和社区审核。
